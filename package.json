{
  "name": "blog.jonasneubert.com",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "description": "",
  "scripts": {
    "tw-build": "tailwindcss -i  ./assets/css/main.css -o ./assets/css/style.css --minify",
    "tw-dev": "tailwindcss -w -i ./assets/css/main.css -o ./assets/css/style.css",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "yarn tw-dev & hugo server",
    "build": "yarn tw-build && hugo --minify"
  },
  "dependencies": {
    "@tailwindcss/typography": "^0.5.19",
    "prettier-plugin-go-template": "^0.0.15",
    "tailwindcss": "^3.4.17"
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  },
  "packageManager": "yarn@4.12.0"
}
